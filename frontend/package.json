{
  "name": "frisor-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "serve": "^14.2.1"
  },
  "scripts": {
    "start": "serve -s public -l 3000",
    "build": "echo \"Build completed\"",
    "test": "echo \"No tests to run\"",
    "dev": "serve -s public -l 3000"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
